import { AppFileurlPipe } from './app.fileurl.pipe';

describe('AppFileurlPipe', () => {
  it('create an instance', () => {
    const pipe = new AppFileurlPipe();
    expect(pipe).toBeTruthy();
  });
});
